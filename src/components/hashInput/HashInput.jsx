import React from 'react';
import { styled } from 'styled-components';

const HashInput = () => {
  return <OnlyHashInput>HashInput</OnlyHashInput>;
};

export default HashInput;

const OnlyHashInput = styled.input``;
